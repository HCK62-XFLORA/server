const { io } = require(`../bin/www`)

// io.on(`connection`, (socket) => {
//     console.log(`A user connected with id ${socket.id}`)
// })
// // io.on(`connection`, (socket) => {
// //     console.log(`A user connected with id ${socket.id}`)
// // })